"use client";
import { useState } from "react"; import { doc,setDoc,serverTimestamp } from "firebase/firestore"; import { db } from "@/lib/firebase/client";
export default function UserManager(){const [form,setForm]=useState({uid:"",name:"",email:"",role:"editor",rtId:"",isActive:true});const [status,setStatus]=useState("");async function save(){if(!db||!form.uid||!form.name||!form.email){setStatus("UID, nama, dan email wajib diisi.");return;}try{await setDoc(doc(db,"users",form.uid),{name:form.name,email:form.email,role:form.role,rtId:form.rtId||null,isActive:form.isActive,updatedAt:serverTimestamp()},{merge:true});setStatus("Profil pengguna berhasil disimpan. Pastikan akun Auth dengan UID yang sama sudah dibuat.");}catch(e){setStatus(e instanceof Error?e.message:"Gagal menyimpan pengguna. Hanya superadmin yang dapat melakukan ini.")}}return <><div className="admin-title"><h1>Pengguna Admin</h1><p>Kelola profil dan hak akses pengguna yang sudah dibuat pada Firebase Authentication.</p></div><section className="admin-panel"><div className="demo-box">Buat akun Email/Password terlebih dahulu di Firebase Authentication. Salin UID akun, lalu isi formulir ini. Halaman ini tidak membuat kata sandi baru.</div><div className="form-columns" style={{marginTop:20}}><div className="form-group form-span-2"><label>UID Firebase Authentication</label><input className="form-control" value={form.uid} onChange={e=>setForm({...form,uid:e.target.value})}/></div><div className="form-group"><label>Nama</label><input className="form-control" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="form-group"><label>Email</label><input className="form-control" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="form-group"><label>Peran</label><select className="form-control" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="superadmin">superadmin</option><option value="editor">editor</option><option value="operator_rt">operator_rt</option></select></div><div className="form-group"><label>ID RT, khusus operator RT</label><input className="form-control" value={form.rtId} onChange={e=>setForm({...form,rtId:e.target.value})}/></div><label className="form-check form-span-2"><input type="checkbox" checked={form.isActive} onChange={e=>setForm({...form,isActive:e.target.checked})}/><span>Akun aktif</span></label>{status?<div className="demo-box form-span-2">{status}</div>:null}<div className="form-span-2"><button className="btn btn-primary" onClick={save}>Simpan Profil Pengguna</button></div></div></section></>}
