"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import styles from "./PublicServicePortal.module.css";

const LETTERS=["Surat Keterangan Domisili","Surat Keterangan Usaha","Surat Pengantar SKCK","Surat Keterangan Tidak Mampu","Surat Keterangan Kematian","Surat Keterangan Kelahiran","Surat Keterangan Belum Menikah","Surat Keterangan Ahli Waris","Surat Keterangan Penghasilan","Surat Pengantar","Surat Keterangan Lainnya"];

export default function LetterRequestPage(){
 const [loading,setLoading]=useState(false); const [error,setError]=useState(""); const [ticket,setTicket]=useState("");
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");setTicket("");const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());try{const res=await fetch("/api/layanan-surat",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await res.json();if(!res.ok)throw new Error(data.error||"Gagal mengirim permohonan.");setTicket(data.ticketId);e.currentTarget.reset();}catch(err){setError(err instanceof Error?err.message:"Gagal mengirim permohonan.");}finally{setLoading(false)}}
 return <main className={styles.page}><section className={styles.hero}><div className={`container ${styles.heroInner}`}><div><span className={styles.eyebrow}>Pelayanan Digital</span><h1>Permohonan Surat</h1><p>Kirim data awal permohonan secara daring. Petugas kelurahan akan memeriksa kelengkapan dan memprosesnya sesuai prosedur pelayanan.</p></div><div className={styles.heroCard}><strong>Simpan nomor permohonan</strong><span>Nomor tiket diperlukan untuk mengecek perkembangan permohonan. Tiket tidak memuat NIK atau nomor KK.</span></div></div></section><section className={styles.section}><div className={`container ${styles.grid}`}><div className={styles.card}><h2>Form Permohonan</h2><p>Isi data dengan benar. Dokumen asli atau berkas tambahan tetap dapat diminta oleh petugas.</p><form className={styles.form} onSubmit={submit}><input className={styles.honeypot} name="website" tabIndex={-1} autoComplete="off"/><div className={styles.field}><label>Nama lengkap</label><input name="name" required maxLength={120}/></div><div className={styles.field}><label>NIK</label><input name="nik" required inputMode="numeric" pattern="[0-9]{16}" maxLength={16} placeholder="16 digit"/></div><div className={styles.field}><label>No. KK</label><input name="kk" inputMode="numeric" pattern="[0-9]{16}" maxLength={16} placeholder="Opsional"/></div><div className={styles.field}><label>RT</label><select name="rt" defaultValue=""><option value="">Pilih RT (opsional)</option>{Array.from({length:13},(_,index)=>String(index+1).padStart(2,"0")).map(rt=><option value={rt} key={rt}>RT {rt}</option>)}</select></div><div className={styles.field}><label>No. HP / WhatsApp</label><input name="phone" required maxLength={40}/></div><div className={styles.field}><label>Jenis surat</label><select name="letterType" required defaultValue=""><option value="" disabled>Pilih jenis surat</option>{LETTERS.map(v=><option key={v}>{v}</option>)}</select></div><div className={`${styles.field} ${styles.full}`}><label>Keperluan</label><textarea name="purpose" required maxLength={700}/></div><div className={`${styles.actions} ${styles.full}`}><button className={styles.button} disabled={loading}>{loading?"Mengirim...":"Kirim Permohonan"}</button><Link className={`${styles.button} ${styles.secondary}`} href="/cek-surat">Cek Status</Link></div></form>{error&&<div className={`${styles.notice} ${styles.error}`}>{error}</div>}{ticket&&<div className={styles.ticket}><span>Nomor permohonan Anda</span><strong>{ticket}</strong></div>}<div className={styles.privacy}>Data identitas dikirim ke sistem internal kelurahan dan tidak ditampilkan pada halaman publik. Jangan membagikan NIK atau No. KK melalui media sosial.</div></div><aside className={styles.card}><h2>Alur Layanan</h2><div className={styles.sideList}><div className={styles.sideItem}><strong>1. Kirim data awal</strong><span>Isi identitas dan jenis surat yang diperlukan.</span></div><div className={styles.sideItem}><strong>2. Verifikasi petugas</strong><span>Petugas memeriksa data dan dapat meminta kelengkapan tambahan.</span></div><div className={styles.sideItem}><strong>3. Proses surat</strong><span>Status dapat dicek menggunakan nomor permohonan.</span></div><div className={styles.sideItem}><strong>4. Selesai</strong><span>Petugas memberi informasi pengambilan atau tindak lanjut.</span></div></div></aside></div></section></main>
}
