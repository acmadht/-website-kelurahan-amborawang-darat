"use client";
import { FormEvent, useState } from "react";
import styles from "./PublicServicePortal.module.css";

type Props={kind:"surat"|"pengaduan"};
export default function StatusCheckPage({kind}:Props){const isLetter=kind==="surat";const [id,setId]=useState("");const [data,setData]=useState<Record<string,string>|null>(null);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");setData(null);try{const res=await fetch(`${isLetter?"/api/cek-surat":"/api/cek-pengaduan"}?id=${encodeURIComponent(id)}`,{cache:"no-store"});const j=await res.json();if(!res.ok)throw new Error(j.error||"Data tidak ditemukan.");setData(j);}catch(err){setError(err instanceof Error?err.message:"Data tidak ditemukan.");}finally{setLoading(false)}}return <main className={styles.page}><section className={styles.hero}><div className={`container ${styles.heroInner}`}><div><span className={styles.eyebrow}>Pelayanan Digital</span><h1>{isLetter?"Cek Status Surat":"Cek Status Pengaduan"}</h1><p>Masukkan nomor tiket yang diterima saat pengajuan. Halaman ini hanya menampilkan informasi status yang aman untuk publik.</p></div></div></section><section className={styles.section}><div className="container"><div className={styles.card} style={{maxWidth:760,margin:"0 auto"}}><h2>Nomor Tiket</h2><form className={styles.form} onSubmit={submit}><div className={`${styles.field} ${styles.full}`}><label>{isLetter?"Nomor permohonan":"Nomor pengaduan"}</label><input value={id} onChange={e=>setId(e.target.value.toUpperCase())} placeholder={isLetter?"SR-YYYYMMDD-XXXXXX":"PG-YYYYMMDD-XXXXXX"} required/></div><div className={styles.full}><button className={styles.button} disabled={loading}>{loading?"Memeriksa...":"Cek Status"}</button></div></form>{error&&<div className={`${styles.notice} ${styles.error}`}>{error}</div>}{data&&<div className={styles.statusBox}><div className={styles.statusRow}><span>Nomor tiket</span><strong>{data.ticketId}</strong></div><div className={styles.statusRow}><span>{isLetter?"Jenis surat":"Kategori"}</span><strong>{isLetter?data.letterType:data.category}</strong></div><div className={styles.statusRow}><span>Status</span><strong>{data.status}</strong></div>{isLetter&&data.letterNumber&&<div className={styles.statusRow}><span>Nomor surat</span><strong>{data.letterNumber}</strong></div>}{(data.note||data.publicNote)&&<div className={styles.statusRow}><span>Keterangan petugas</span><strong>{data.note||data.publicNote}</strong></div>}{data.completedDate&&<div className={styles.statusRow}><span>Tanggal selesai</span><strong>{data.completedDate}</strong></div>}{isLetter&&data.verificationUrl&&<div className={styles.statusRow}><span>Verifikasi dokumen</span><strong><a href={data.verificationUrl}>Buka halaman verifikasi</a></strong></div>}</div>}</div></div></section></main>}
